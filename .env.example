# @CODE:INFRA-001:DATA | SPEC: SPEC-INFRA-001.md
# 환경 변수 템플릿 - 실제 사용 시 .env 파일로 복사하고 비밀번호 변경

# PostgreSQL 설정
POSTGRES_USER=liaruser
POSTGRES_PASSWORD=change-this-password
POSTGRES_DB=liardb

# Redis 설정
REDIS_PASSWORD=change-this-redis-password

# MinIO 설정
MINIO_ROOT_USER=minio-admin
MINIO_ROOT_PASSWORD=change-this-minio-password

# 보안 권장사항:
# - POSTGRES_PASSWORD: 최소 16자, 대소문자+숫자+특수문자 조합
# - REDIS_PASSWORD: 최소 32자, 무작위 생성 권장
# - MINIO_ROOT_PASSWORD: 최소 8자, MinIO 콘솔 로그인용
